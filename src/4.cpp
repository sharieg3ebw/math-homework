#include <iostream>
#include <cmath>
using namespace std;

int main() {
    double x = 0; // set x to zero
    while (x <= 10) { // while loop condition
        cout << "x is equal to: " << x << endl; // print x value
        x += 0.25; // add 0.25 to x
    }
    return 0;
}
